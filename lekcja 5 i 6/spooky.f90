program a
implicit none 
	character(len=100)::t(28)
	character(len=100)::k
	integer::i,nmax
	integer::j(28,2)
	logical z
	nmax=28
	do i=1,nmax
		read(*,'(A100)'),t(i)
		print*,len(t(i)),len_trim(t(i))
		j(i)=len_trim(t(i))
		
	end do	
        do i=1,nmax
	do while( 
		z
		do p=1,nmax
		if(j(i) )
		end do
	end do	
	
	end
	
